##### linux/Android Requires Install #######

lklfuse -o type=ext4 /dev/da0 /mnt

jmtpfs -o allow_other /mnt


### No Install Required ################################

FOR USB FORMATTED WITH FAT32

# gpart destroy -F da0
# gpart create -s mbr da0
# gpart add -t fat32 da0
# newfs_msdos -F32 -L usb-drive /dev/da0s1

mount -t msdosfs /dev/da0s1 /mnt

################################

# UFS, obviously

mount -t ufs /dev/da0s1 /mnt

